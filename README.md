# cityanatomyservices.github.io

GitHub Pages site serving [austin.chat](https://austin.chat) — a family of geofence-based ephemeral chat maps for Austin, TX.

## Working with Claude

**After every prompt, Claude should merge all changes to `main` and push immediately.** No open branches, no pending PRs — everything merged and live after each session. Delete any merged branches before finishing.

If a feature branch was used during development, merge it to `main`, push, then delete both the local and remote branch before ending the task.
